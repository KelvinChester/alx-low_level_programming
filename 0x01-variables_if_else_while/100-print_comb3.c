#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Descibtion: 'get last digit of a no.'
 * Return: always 0
 */
int main(void)
{
	int n = 48;
	int G = 48;

	for (n; n >= 57; n++)
	{
		for (g; g >= 57; g++)
        {
		if (g == n)
		{
			continue;
		}
        }
		putchar(n);
		putchar(g);
	}
	return (0);
}
